# 潮间 v2：看穿海面

## 要解决的问题

第一版把钓组、漂相和训练拆成独立知识模块。第二版围绕一次真实操作组织学习：抛投、下沉、撒饵、含饵、绷线、漂动、判断、回放。用户的动作、眼前的动画和复盘证据来自同一个状态。

## 入口与运行

- 本地：`npm run dev` 后打开 `/v2/`。
- GitHub Pages：`https://mcmcmcmmmc.github.io/abo-fishing-lab/v2/`。
- 原版继续在根路径；Vite 同时构建两个 HTML 入口。
- `npm test` 验证模型，`npm run build` 验证 TypeScript 和双入口静态产物。

## 两种体验

探索模式在同一海域调咬铅、钓棚、流速，额外提供风、浪和阿波承载量。点击海面选落点、撒诱饵、点击钓组查看作用；透视与垂直参照可以开关。初始不计时，首次抛投后开始，最长 32 秒，可随时暂停。

盲判模式在鱼带饵、浪涌、风压主线、触底四类规则场景中选择一类。判断前隐藏水下绘制、实时提示、参数与事件；选择推断原因后，执行扬竿或结束等待。复盘展示已经发生的证据、用户推断与实际记录，支持 0.5 倍回放、拖动时间轴和跳到事件前一秒。不用伪造分数代替证据。

## 模型和边界

`src/v2/model.ts` 使用固定 30 Hz 步长。配重影响下沉速度、流中倾斜和剩余承载量；钩饵接触同一渲染海底曲线；诱饵独立下沉和漂移；鱼经历接近、试探、含饵、带饵、离开。持续牵引经简化延迟到达阿波，鱼含饵前或子线未拉紧时扬竿不能判为成功。环境案例不会同时触发鱼含饵。

`useOcean.ts` 保存约 10 Hz 的不可变快照；回放按实际时间戳查找该次记录。Canvas 只绘制，不推进状态。波浪、主线形变、漂相放大镜与主场景使用同一世界状态。

这是明确标注的教学近似，非流体力学或完整钓线张力求解。参数为模型输入，鱼是规则代理，距离按教学比例呈现，不表示真实钓场预测、装备承载保证或中鱼概率。改变参数会在探索中实时生效，但回放保留当时值。

## 验证

自动化测试覆盖：相同输入重放一致、配重改变下沉和浮力、流中几何、鱼含饵先于漂信号、早扬竿失败、三类假信号没有暗藏鱼口、事件发生前不揭示预设原因、超载自行下沉、快照隔离、海底碰撞约束。原版的 16 项测试保留。

浏览器验收应检查首次抛投、撒饵、参数调整、透视、盲判隐藏与揭示、回放、390 px 手机布局和静态部署子路径。
