# 验证记录

日期：2026-09-20。此记录是本次实现者的自动化与浏览器检查，不是独立钓法专家验收，也不是实际海钓验证。

## 自动化

- `npm run build`：TypeScript 检查和 Vite 生产构建通过。
- `npm test`：3 个文件、16 项测试通过。
- 物理 7 项：加铅的单调关系；同速水层无风的垂直线组；侧风方向；过载与海底边界；30 / 120 fps 固定步长一致性；非法输入约束；潮流改变同时影响漂移和倾角。
- 信号 4 项：6+9 情境覆盖；信号传播前不提前响应 / 所有样本确实运动；回放可重复且值有限；关键事件随时间回退正确。
- 训练 5 项：不立即重复；原因与动作独立评价；合理保留判断；损坏存储恢复；有效进度往返解析。

## 真实浏览器交互

在 Codex 内置浏览器中通过界面进行：

- 点击移除棉线结，页面切换为恢复按钮，并显示“限位消失，继续下沉”。
- 检查 11 个 SVG 部件均可通过可访问按钮定位。
- 三视图均显示，同步时间推进；调节风速和风向，并暂停。
- 配重从 1 调到 3 g 等效：露出比例 25% → 0%，承载余量 0.58 → -1.42 g，下沉速度 0.30 → 0.45 m/s。
- 展开水下真相；点击 2.0 s 吸饵事件后，时间轴与当前事件同步定位。
- 训练开始时水下图未挂载，答案及拖动时间轴被禁用。
- 完整观察 8 秒后作答“信息不足 + 等待”；提交后解锁水下、慢放、因果解释，计数增加一次，重复提交按钮禁用。
- 刷新页面后，练习次数与分项结果仍保留。
- 1440 px 桌面布局与 390 px 窄屏基础检查；窄屏文档宽度等于视口宽度，无整页横向溢出（不等于真实手机验证）。
- 页面控制台未发现 warning / error。

## 限制与未验收项

- 没有进行真实移动设备、其他浏览器引擎、长期性能 / 大规模无障碍测试。
- 未证明教学学习时长、识别准确率或真实垂钓收益。
- 未验证真实渔具规格、海况、浮力 / 阻力系数、鱼的生态行为。
- Phase 6 / 7 仅为明确的待开发入口，不计入已完成功能。

## 修复过的问题

- 依赖下载和本地监听受沙箱限制，使用获准的命令启动后恢复；没有依赖外部云服务。
- 修正 SVG 交互部件被整体 image 语义遮蔽的问题。
- 修正参数默认值与 slider 步进不一致的问题。
- 补充窄屏图标导航名称与页面切换滚动复位。
- 潮流参与表 / 底层速度混合的权重分开，保证调节总体潮流时水下角度也可响应。
